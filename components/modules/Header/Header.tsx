import clsx from "clsx"
import styles from "@/styles/header/index.module.scss"

interface HeaderProps {}

const Header = () => {
  return <header className={clsx(styles.header)}>f</header>
}

export default Header
